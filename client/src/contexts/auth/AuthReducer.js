import {
	REGISTER_SUCCESS,
	REGISTER_FAIL,
	LOGIN_SUCCESS,
	LOGIN_FAIL,
	AUTH_ERROR,
	USER_LOADED,
	LOGOUT,
	CLEAR_ERRORS

} from '../reducerTypes';

export default (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}