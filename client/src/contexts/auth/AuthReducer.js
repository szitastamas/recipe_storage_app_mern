import {
    REGISTRATION_SUCCESS,
    REGISTRATION_FAIL,
    LOGIN_SUCCESS,
    GET_USER
} from '../reducerTypes';

export default (state, action) => {
    switch(action.type){
        default:
            return state;
    }
}